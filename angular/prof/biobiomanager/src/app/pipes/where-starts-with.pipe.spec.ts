import { WhereStartsWithPipe } from './where-starts-with.pipe';

describe('WhereStartsWithPipe', () => {
  it('create an instance', () => {
    const pipe = new WhereStartsWithPipe();
    expect(pipe).toBeTruthy();
  });
});
